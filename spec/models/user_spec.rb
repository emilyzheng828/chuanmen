#require 'rails_helper'

#RSpec.describe User, type: :model do 

#describe "#find_other_available_users_for" do

  #it "will return no overlapping" do
    #user1_freetime = [AvailableTime.new(from: DateTime.new(2016, 8, 10), to: DateTime.new(2016, 8, 12))]
    #user2_freetime = [AvailableTime.new(from: DateTime.new(2016, 8, 15), to: DateTime.new(2016, 8, 30))]
    #expect(overlapping(user1_freetime, user2_freetime)).to eq("no overlapping")

    #expect(user1.find_other_available_users_for(date)).to eq([user2, user3])

  #end

#end

#describe "#find_other_available_users_for" do

  #it "will return no overlapping" do
    #user1_freetime = [AvailableTime.new(from: DateTime.new(2016, 8, 10), to: DateTime.new(2016, 8, 16))]
    #user2_freetime = [AvailableTime.new(from: DateTime.new(2016, 8, 15), to: DateTime.new(2016, 8, 30))]
    #expect(overlapping(user1_freetime, user2_freetime)).to eq("no overlapping")

    #expect(user1.find_other_available_dates_with(user2)).to eq(["2016, 8, 15", "2016, 8, 16"])

  #end


#end